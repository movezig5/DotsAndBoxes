package enums;

/* ***********
 * Enum: Error
 * ***********
 * Description:
 * 		An enum representing the result of performing an action.
 * 		Useful for telling the user what went wrong, or for diagnosing problems.
 */

public enum Error {
	SUCCESS,
	OUT_OF_BOUNDS,
	SPACE_FILLED,
	INVALID_SPACE,
	NULL_NODE,
	INVALID_NUMBER
}
